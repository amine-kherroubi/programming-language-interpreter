{
    let int a, c = 10, 100
    let float b = 3.2
    keep bool flag1, flag2 = true, false

    proc doNothing() {
        let int x = 1
        let float y = 2.0
    }

    func multiply(x: int, y: float) -> float {
        let float result = x * y
        give result
    }

    exec doNothing()

    let float result = multiply(a, b)
}